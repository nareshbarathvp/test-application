import { OptionValidateDirective } from './option-validate.directive';

describe('OptionValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionValidateDirective();
    expect(directive).toBeTruthy();
  });
});
